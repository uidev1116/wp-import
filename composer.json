{
  "name": "uidev1116/acms-wp-import",
  "description": "WordPress のデータを a-blog cms にインポートするための拡張アプリです。",
  "scripts": {
    "lint": [
      "vendor/bin/phpcs --standard=phpcs.xml || true"
    ],
    "format": [
      "vendor/bin/phpcbf --standard=phpcs.xml"
    ]
  },
  "require-dev": {
    "squizlabs/php_codesniffer": "^3.7"
  }
}
